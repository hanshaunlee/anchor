# Database archive

One-off or sample SQL scripts preserved for reference. Not run by migrations or main tooling.

- **setup_alerts_one_household.sql** — Example: enable Notify + Action plan for a single household (replace UUIDs with yours). For generic setup use `scripts/enable_alerts_setup.py` instead.
